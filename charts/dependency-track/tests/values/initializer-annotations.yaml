database:
  existingSecret:
    name: dt-db
secretManagement:
  database:
    kek:
      existingSecret:
        name: dt-kek
apiServer:
  initializer:
    enabled: true
    annotations:
      helm.sh/hook-delete-policy: "before-hook-creation,hook-succeeded"
